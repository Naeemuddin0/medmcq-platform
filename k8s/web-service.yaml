apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    app: medmcq-web
spec:
  type: NodePort
  selector:
    app: medmcq-web
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
